// given the previous mouse position on the canvas, zoom level and screen size, what is the next offset
export function getOffset(mousePosition: { x: number, y: number}, zoomFactor: number, canvasSize: number, containerWidth: number, containerHeight: number) {
 return {
     x: 0,
     y: 0,
 }
}